#include <string>

/* Determine if a number is a palindrome or not */
class Solution
{
public:
    /*
     * Attempt to solve without converting integer to a string
     */
    bool isPalindrome(int x) 
    {
        bool result = false;
        if (x < 0)
        {
            result = false;
        }
        else 
        {
            
        }
        return result;
    }
};

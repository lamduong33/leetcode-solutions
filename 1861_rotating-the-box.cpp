#include <vector>

class Solution
{
public:
    std::vector<std::vector<char>>
    rotateTheBox(std::vector<std::vector<char>> &box)
    {
        std::vector<std::vector<char>> result;

        int m = box.size(); // row
        int n = box[0].size(); // column

        return result;
    }
};
